import React from 'react'

import '../App.css'

export default function SkillLabel({ title }) {
    return (
        <div className = "Skill_Label Center">
            { title }
        </div>
    )
}