import React from 'react'

import '../App.css'

export default function SkillPercentage({ percent }) {
    return (
        <div className = "Skill_Percentage Center">
            { percent }
        </div>
    )
}